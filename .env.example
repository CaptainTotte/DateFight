# No global admin password — each event gets its own admin code at creation.

# Demo codes: set these to pre-existing event/admin codes to show "Try demo"
# buttons on the landing page. Leave blank to disable.
DEMO_EVENT_CODE=
DEMO_ADMIN_CODE=
