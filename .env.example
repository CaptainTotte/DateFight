# No global admin password — each event gets its own admin code at creation.

# Demo event: if both codes are set, the backend creates a "Demo" event with
# these exact codes on startup (if it doesn't already exist). Leave blank to skip.
DEMO_EVENT_CODE=
DEMO_ADMIN_CODE=
