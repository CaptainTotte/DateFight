# No global admin password — each event gets its own admin code at creation.

# Demo codes for manual testing. No UI effect — just a handy reference.
# Leave blank if not needed.
DEMO_EVENT_CODE=
DEMO_ADMIN_CODE=
