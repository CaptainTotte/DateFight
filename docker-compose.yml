services:
  api:
    build: ./backend
    environment:
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin123}
      DB_PATH: /data/samdag.db
    volumes:
      - db-data:/data
    restart: unless-stopped

  web:
    image: nginx:1.27-alpine
    ports:
      - "3000:80"
    volumes:
      - ./frontend:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - api
    restart: unless-stopped

volumes:
  db-data:
